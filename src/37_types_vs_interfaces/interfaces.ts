(() => {
  interface InputProps {
    type: 'text' | 'email',
    value: string,
    onChange: (newValue: string) => void
  }

  /**
   * Interfaces support:
   * - Declaration Merging
   * - Extends (alt intersections)
   * */
})()